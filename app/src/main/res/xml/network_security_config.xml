<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- The app talks to a local Flask server over HTTP during LAN discovery. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>

